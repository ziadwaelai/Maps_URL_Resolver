services:
  maps-api:
    build: .
    image: maps-url-resolver
    container_name: maps-api
    ports:
      - "8001:8000"
    restart: unless-stopped
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-4o-mini}
