# Copy to `.env` and fill in. docker-compose picks it up automatically.
# Leave blank / delete the file to disable AI enrichment.

OPENAI_API_KEY=sk-...
# Default model. gpt-4o-mini is cheap & good for short JSON.
OPENAI_MODEL=gpt-4o-mini
